# DIVERGENCE LEDGER — C# ↔ C++ field-level

Living, field/byte-level list of every divergence between `ReCap.Server` (C#) and the C++ ground truth (`ReCapCpp`), walked along the single-player Dungeon critical path. The macro index stays in [`PORTING_MATRIX.md`](PORTING_MATRIX.md) (class/handler level); **this is the micro level** (field/byte).

Method & rationale: [`../../superpowers/specs/2026-05-31-port-fidelity-plan-design.md`](../../superpowers/specs/2026-05-31-port-fidelity-plan-design.md). Verified protocol truths: [`../VERIFIED_FACTS.md`](../VERIFIED_FACTS.md).

## How to use
- One row per divergence of a **field/packet** (not a whole class).
- **Status:** `open` (found via source-diff) → `wire-confirmed` (seen in capture) → `fixed` (code changed, one commit) → `verified` (wire-parity + client advances).
- One `fixed` item = **one commit** (bisect-friendly). Cite the commit short hash.
- Refs are `file:line`. C++ root: `…/ReCapCpp/darkspore_server/source`. C# root: `ReCap.Server/`.

## Critical-path steps (walk order)
1 Login/Blaze · 2 Lobby · 3 REST account · 4 RakNet connect/HelloPlayer · 5 Hello LPU · 6 Spaceship→ChainVoting · 7 ChainVoting · 8 Vote→PreDungeon · 9 PreDungeon · 10 Dungeon entry · 11 Frame loop

## Ledger

| ID | System | Step | C++ ref | C# ref | Divergence | Status | Commit |
|----|--------|------|---------|--------|------------|--------|--------|
| D-001 | LabsCharacterData | 5/8 | Character::WriteTo (Character.cpp:99) | LabsPlayerUpdatePacket.cs:194 | `mPartAttributes` only 6 of ~110 filled (rest zero); C++ fills full array from ClassAttributes. Low crash-risk (C++ also sparse) but a real de-hardcode target. | open | |
| D-002 | LabsCharacterData | 8 | Character::WriteTo (Character.cpp:99) | LabsPlayerUpdatePacket.cs:215 | GearScore: C++ wire shows 300.0; C# uses DB value (may differ). Verify intended. | open | |
| D-003 | Deck HUD (client) | 11 | — (cPlayerDeck::UpdateHud @0x51b860) | Game.cs / LPU | `cPlayerDeck+0x20` (Scaleform movie) null → crash @0x551f47. **output.txt 2026-05-31 confirms: client reaches Dungeon (GameState 0x06), spawns 3 heroes, deploys deck=1, runs frame loop ~4s (GameState ×181→×221), does mid-game joinRoom+getPartList (in-game deck/squad UI), then disconnects @16:15:23.** Insensitive to D-004 (dataBits) and to all login/lobby/REST changes — client passes steps 1-10. Root is in-game step 11. Next: Ghidra runtime debugger at 0x551f10 / where cPlayerDeck+0x20 is set. | open | |
| D-004 | Player hello dataBits | 5 | Player ctor + Setup() (Player.cpp:58-66, 178-206) | LabsPlayerData.SetInitialDataBits (LabsPlayerUpdatePacket.cs:93) | C# initial dataBit set omitted `13 CrystalData`, `14 CrystalBonuses`, `17 ChainProgression`. C++ hello union = {0,3,4,5,6,7,8,12,13,14,15,16,17,18,21,22}. Aligned C# + golden test to it. **Verify gate (client, 2026-05-31): crash PERSISTS** — D-004 is a fidelity fix, NOT the crash cause. | fixed | 2a43dd1 |
| D-005 | LPU/reflection endianness | 5/11 | Write<T> wrapper (Types.h:218-226) + include-order (Server.h:9/20, Types.h:11) | LabsPlayerUpdatePacket.cs, ReflectionSerializer.cs | A BE re-read of `#define __BITSTREAM_NATIVE_END` was refuted; include order makes it a no-op → wire LE. C# (BinaryWriter) already LE = correct; stale BE comments + 5 golden tests corrected to LE. | verified | fee05f8 |

| D-006 | REST inventory parts | 3 | Part/Parts::WriteApi (Part.cpp:89,314-319 — `id`/`reference_id` = 1-based vector position `++index`) | GameRestController getPartList + CreaturePartMapper (`id`/`reference_id` = DB row ID) | C++ uses a 1-based position in the parts vector; C# uses the DB primary key. **VERIFIED BENIGN 2026-05-31:** field set matches C++ 1:1; both are stable id↔part identifiers and C# is internally consistent (getPartList sends DB id; updatePartStatus/vendorParts look up by DB id). Satisfies the client contract per the project's "match the contract, not C++'s byte quirk" rule. NOT a bug; do not change (would only add transaction-mapping risk). | verified | — |
| D-007 | REST deck/account extras | 3 | User.cpp:611-618; User.cpp:141 (`grant_online_access` commented out) | DeckMapper / AccountContract | C# emits extra `png_*_url` inside `<deck><creatures>` and `grant_online_access`. **VERIFIED BENIGN:** client tolerates extra tag-addressed fields; not on the crash path. | verified | — |

**Step-3 (REST account) source-diff done 2026-05-31:** squad-critical fields (`noun_id`, `gear_score`, `id`, `version`) on `<creature>` and `<deck>` nodes match C++ field-for-field. Only the low-risk extras above (D-006/D-007) diverge. This corroborates VERIFIED_FACTS that the deck-HUD crash (D-003) is **game-state/LPU-driven, not REST** — the REST account payload is essentially aligned. Wire-diff of step 3 still pending (HTTP/XML, byte-aligned, no RakNet parser needed).

**Step-1/2 (Blaze login/lobby) source-diff done 2026-05-31 — ALL candidates `open (source-diff, unverified)`.** Critical caveat: `output.txt` 2026-05-31 proves the client **passes login + lobby + reaches the Dungeon**, so none of these are blockers — they are fidelity divergences, NOT the crash. Agent-sourced; verify each against C++ `file:line` before trusting/fixing (prior agents have over-claimed "critical"). Candidates to triage later:

- Step 1 (auth): SilentLogin/ExpressLogin — C++ `WriteFullLogin` wraps `SESS` struct + outer `AGUP/NTOS/PCTK/PRIV/SPAM/THST/TURI` (AuthComponent.cpp:613,636) vs C# bare `SessionInfo` (AuthenticationComponent.cs:287,315); LookupUser tag `EDAT` (Functions.cpp:499) vs C# `DATA` (UserSessionsComponent.cs:401); GetTOSInfo missing `PRIV/THST/TURI` (AuthComponent.cpp:509); `getTelemetryServer 0x05` unhandled in C# (UtilComponent.cs:15); redirector `SECU` int vs C# bool; `TMOP` OptIn(C++) vs OptOut(C#). Client passes login regardless.
- Step 2 (lobby): FinalizeGameCreation reply `{GID}` (GameManagerComponent.cpp:1105) vs C# empty (GameManagerComponent.cs:44); `SelectPseudoRoomUpdates 0xA0` unhandled in C#; NotifyGameSetup REAS inner-struct `"VALU"` label (needs TdfEncoder.cs check); C# extra `QUEU`/`GURL`/`MATR`/`JGS` fields C++ omits. Client reaches Dungeon regardless.

| D-008 | LPU character block | 5/8 | Character::WriteTo (Character.cpp:99) | LabsCharacterData.WriteTo | **EXONERATED via wire-diff.** cpp vs cs 5043B SetSquad LPU (cmp_lpu.py): 0 both-nonzero diffs; 96B = C++ heap-garbage-in-gaps (benign), 38B = C# redundantly filling partAttributes that C++ leaves zero (C++ ships them via AttributeDataUpdate 0x96 instead). Every meaningful field is byte-faithful. LPU is NOT the crash. | verified | (wire 2026-05-31) |
| D-009 | In-game object stream | 10/11 | Server.cpp spawn loop (GetActiveObjects) | Game.OnPlayerStart | **Massive divergence (wire-diff cpp_loopback).** C++ spawns **278+ objects** (0x8C×278 + a companion **0x98×278** + 0xB8×172 + 0x94×13 + 0x90×16 + 0x91×12 + 0xA8×2 + 0xB7); C# spawns **only 3 heroes** and sends **zero** 0x98/0x90/0x91/0x94/0xB8/0xA8/0xB7. Prime crash leads (deck HUD): (a) C# hero ObjectCreate **91B** with extra fields `06`(pos vec3)/`07`/`0x10` vs C++ hero **59B minimal** (no pos); (b) every C++ ObjectCreate has a paired **0x98** companion, C# sends none; (c) ObjectUpdate C# 46B vs C++ 19/21B. Nouns match (squad correct). **PARTIAL FIX 5dba20c:** hero ObjectCreate field set → C++ {0,1,3,17} (59B) and ObjectUpdate → {6,16} (21B), golden-tested. **Objectives also byte-matched (d981fcf) but crash UNCHANGED → objectives eliminated.** **KEY FINDING 2026-05-31:** the C++ working log does `joinRoom`/`getPartList` ONCE pre-game; the C# client re-issues them **mid-dungeon (~1s after deploy)** then crashes — i.e. the client is **falling back to a menu/room state** because the C# dungeon is an empty, static world. C++ post-deploy sends a LIVING stream (InteractableDataUpdate×30, LPU×29, ObjectPlayerMove×26, ObjectUpdate×16, ObjectTeleport×16) and the client plays to the C++ frontier (UseCharacterAbility). C# sends only GameState + sparse LPU. **Real fix = the living-world loader (phases below).** Client-verify gate: crash persists. | open | |
| D-010 | Post-deploy fall-back trigger | 11 | client (Darkspore.exe) | — | **EXHAUSTIVE ELIMINATION 2026-05-31:** the client deploys, runs ~1s, sends a mid-game `joinRoom` (fall-back), then crashes ~3.8s after deploy — **identical timing regardless of**: empty world, 2866-object world, per-object 0x98 companion, finalizeGameCreation {GID}, LPU/objectives/hero byte-matches, GameManager sequence match. So the trigger is **NOT in the matched wire content** — it is a client-side condition in the ~1s post-deploy window (timeout / awaited event / living movement). Note: C++ for zelems_1 spawns minimal (teleporters + obelisks + ~1 test enemy); C# over-spawns 2866 (loose classification, `skipped 0`) — beyond C++, but crash is identical with/without, so object load is NOT the cause. **GHIDRA FINDING 2026-06-01 (joinRoom EXONERATED, confidence high):** the mid-game `joinRoom`+`getPartList` are **NOT a fall-back** — they are normal persistent Blaze-lobby traffic (TCP 42125), orthogonal to GameState/RakNet. `joinRoom` = standard lobby **chat-room** subscribe (`cBlazeRoomsManager::joinLobbyRoom@0x00c39910`, log "Joined lobby room"@0106c1b4), fired by Blaze SDK `onRoomViewAdded@0x00c2ea10` (view-available push), no error/timeout guard. `getPartList`@0x00465310 (str `api.inventory.getPartList`) = inventory refresh on a deck-slot check. The ~1s timing is coincidental (lobby poll cycle resuming), not causal. ⇒ **the joinRoom send is a SYMPTOM/background-noise, not the crash trigger; server needs NO joinRoom/getPartList change.** Crash @0x551f47 is a pure ClientUI ViewManager failure (NULL `GFxMovieView*` at subview+0x20), separate from the Blaze channel. Residual lead (low-prob, unverified): `onJoinLobbyRoomResponse@0x00c39660` calls `(*+0x138)` comp 0xc type 2 — could it poke a HUD party/room panel uninitialized in Dungeon mode? **Next: Ghidra threads still unrun — `post-deploy-wait` (which inbound msg sets the HUD-ready flag; prime suspect), `subview-manager`, `view-state-transition`, `movie-load-gate`.** Wire-matching exhausted. | open | | D-005 verified by C++ source (include-order proof) + matches the existing capture claim. D-003 is the live crash (in-game step 11); steps 1-10 are empirically passed by the client.
| D-014 | Deck/squad system | 3/10 | Squad = fixed uint32[3], 0=empty (Squad.h:34); ResetSquads = empty decks (User.cpp:247-258; the SignUp slot-0 fill is a TODO-remove hack, API.cpp:808-814); UpdateSquad validates ownership + compacts (User.cpp:260-289); WriteSquadsAPI skips zero slots (User.cpp:589-620); SetSquad: empty slot -> null character, no completeness check (Player.cpp:267-313) | DeckService/GameService/GameRestController | Rebuilt to the C++ contract: virgin accounts get 3 EMPTY decks; updateDecks ownership-validated, active-slot-only, always 3 positional values; ResolveSquad injection fallback DELETED; CreatureIds SQLite round-trip locked by test (EF Core 9 native JSON column, no converter needed). **One deliberate divergence:** C++ UpdateSquad never clears slots a save no longer references (stale-slot quirk — a removed creature would resurrect on reload); C# zero-fills the remainder so the persisted deck is exactly the client's full-state save (the client sends all 9 slot values per save). Fixes: seeded creature re-appearing in edited decks + "squad incomplete" after relaunch. Spec: docs/superpowers/specs/2026-06-04-deck-system-design.md. | fixed | |
| D-013 | QuickGameMsgs reset byte | 10 | `Server::SendQuickGame` writes `Write<bool>(reset=true)`=0x01 on the Dungeon path (Server.cpp:1199 after DirectorState, before OnPlayerStart; def Server.cpp:2346) | `QuickGameMsgsPacket.WriteTo` wrote `(byte)0`; sent from Game.cs:393 Dungeon case | **Setup-phase divergence (C++ does / C# didn't), prime crash candidate.** C++ sends 0x01 at dungeon entry and the client reaches interactive gameplay; C# sent 0x00. The C++ source comment "if true: set state Spaceship" is the author's admitted guess ("not 100% sure, ignore all") — empirically 0x01 is the dungeon-correct value (C++ client plays the dungeon with it). C# 0x00 may be why the client mis-initializes (ship/map-room cMapRoomUI active → null-movie crash @0x551f47, [[crash-0x551f47-cmaproomui]]). FIX: send `(byte)1`. Runtime packet — no account recreation needed. **✅✅✅ CLIENT-VERIFIED 2026-06-04: CRASH FIXED.** This was THE root cause of the 0x551f47 crash open since 2026-05-30. `reset` byte governs stay-in-dungeon (0x01) vs fall-to-ship/map-room (0x00); C# 0x00 drove the client into the ship state → cMapRoomUI (map-room UI) activated inside the dungeon → its unbound Scaleform movie → null-deref. D-011 (cubes) + D-012 (game-type) were correct fidelity fixes but NOT the crash; D-013 cracked it. Method that found it: C++↔C# setup-phase source-diff after recognizing the crash was insensitive to gameplay content. | **verified** | |
| D-025 | Object death path | 11 | C++ Object death (OnObjectDeath; HP≤0 → removal/`ObjectDelete` 0x8E to client) — exact sender/payload unconfirmed | GameScriptContext.ApplyHeal / ObjectManager / Game | **OPEN (found via in-game gate 2026-06-07).** Combat damage works end-to-end (Pummel rolls 1-5, enemy 24→0, [[lua-system-contract]] combat gate ✅). But **death is not modeled**: at HP 0 the corpse stays in the object table → melee tick falls into `FindBestTargetInArc`, keeps finding the dead obj (arc HIT) while `ValidateHostileTarget(hp>0)` blocks damage → **infinite no-op cast loop on the corpse**; client never sees the enemy die. Two halves: (a) **server-side** = remove/flag object at hp≤0 so it stops being targeted + fire death event (loot/objective hooks) — `ObjectManager.Remove`/`MarkForDelete` exist, safe go-direct; (b) **client-visual** = `ObjectDelete` 0x8E is in the PacketType enum + handled in PacketActivator, but **no packet class, never sent, payload unconfirmed** (objectId only? +death/vaporize flags? death-anim-first vs immediate?) → needs Ghidra (client 0x8E handler) / wire capture before sending (client null-derefs on misread). Next: confirm 0x8E direction+payload, then implement (a)+(b). | open | |
| D-024 | Obelisk invisible (client visual pipeline) | 10/11 | — (client-side; C++ working session never spawned an obelisk, capture has zero samples) | Game.PopulateLevel obelisk pass | **OPEN, parked for Simulation phase (user call 2026-06-05).** Client gate: obelisks invisible at point-blank with BOTH wire shapes (marker-bound {6,7,8,17,22} and bare {6,7}) while enemies (same bare shape) and the SecurityTeleporter (renders its own `Shared!teleporter_level.bmdl` — the visible portal IS our trigger object) work. Ghidra mapped the visual pipeline (VERIFIED_FACTS "Client object visual pipeline"): `ObjectManager::UpdateObjectGraphics` @0x009ec530 routes creature (locomotion!=0 → obj+0x2C0) vs interactable (obstacle handle obj+0x2BC), interactable gates = noun.isFixed && noun.physicsType!=0 && obj.hasCollision. Obelisk noun probe (tools/scratch/NounProbe): isFixed=True ✓, physicsType=enum (value not extracted), `modelKey = prefab_*_obelisk_gfx.Markerset` — a gfx-markerset composition = third visual pipeline, unmapped. Candidates: physicsType value, hasCollision routing into obj+0x60, gfx-markerset modelKey resolution. Decisive next: runtime BP @0x009ec530 with a live obelisk create. Diagnostic logging left in AssetDatabase.LogObeliskNounDiagnostics (prints gate fields at startup). | open (parked) | e24312a |
| D-023 | Teleporter activation + obelisk bare-shape experiment | 11 | C++ NEVER implemented trigger volumes — TriggerVolumeDef is asset schema only (AssetCatalog.cpp:220-249), no onEnter handling anywhere; walking into a teleporter did nothing under C++ either | Game.RegisterTeleporterTrigger/CheckTeleporterTriggers/ResolveMarkerPosition + obelisk pass | **Beyond-C++ feature, justified by client contract:** client resolves teleporter destinations locally (Lua GetTeleporterDestination) but the retail flow needs SOMEONE to execute the teleport; we use the D-015 client-verified pair (0x90 ObjectTeleport + 0x91 flags|=0x20). Server detects entry from the client-streamed hero pos in every ActionCommand header (~5/s while walking; scaldron_4 gate proved the user stood exactly on trigger (-311,380) with zero effect). Radius from TriggerVolumeDef (sphereRadius else box half-extent, else 4); destination via lazy markerId→pos index over all markersets; 3s per-object cooldown kills A↔B ping-pong. **Obelisk experiment:** capture has ZERO obelisk creates (C++ session never spawned one) and marker-bound {6,7,8,17,22} stayed invisible at point-blank — switched obelisks to the only render-verified world shape (bare {6,7}, bindMarker:false). ✅ CLIENT-VERIFIED 2026-06-05: enemies render + teleporter teleports ("teleporter e inimigos aparecem, teleporter funciona"). Obelisk still invisible → split to D-024. Follow-up same day: teleport FX wired — trigger `events.onEnterEvent/onExitEvent` (.ServerEventDef keys) now fired via ServerEvent 0x9B (client-verified contract, see VERIFIED_FACTS) on the hero at source and after the snap. | **verified** (enemies+teleport) | fb43b77 |
| D-022 | World-object ObjectCreate shape (enemies/obelisks invisible) | 10/11 | cpp_loopback decoded in full (tools/scratch/decode_creates.py): 284× 0x8C = 3 heroes 59B {0,1,3,17} + 3 enemies 81B **{6,7} only** + 278 SecurityTeleporter 93B {6,7,8,17,22}; ALL createData rot fields zero (marker rotDegrees never propagated; rotY=-0.0 sign quirk); enemy createData scale=1/team=0/hasColl=1; assetId=0 (client falls back to noun's own, CreateObjectInternal @0x009d6f8c). **C++ session spawned ZERO obelisks** — obelisk invisibility is not a C# regression | Game.SpawnWorldObject + WorldObjectCreateTests | User gate: obelisks/NPCs don't render (only deck heroes). Wire-diff found C# enemy creates carried {6,7,8,17,22}+SpawnPoint markerId + real marker rot — C++ sends bare {6,7}. FIX: enemies now bindMarker:false ({6,7}, identity quat); all world objects stop writing rot into createData; spawn positions logged. Ghidra client trace (OnGmsObjectCreate @0x0053f550): silent-drop gates = duplicate objectId (entry gate), createData parse fail, **gameplay component obj+0x2A8 null (noun-type-driven — the likely SecurityTeleporter no-op, harmless: triggers are scale-0 invisible by design)**, graphics component obj+0x2c0 null; rot/markerId/extra-fields all NON-fatal (markerId remap of unknown id writes 0; client converts Euler→quat @0x0053d730). Noun hash domain re-validated: FNV(name+ext, lowercase) reproduces all capture nouns exactly (SecurityTeleporter 0x2983C017, NomadWithDrone 0x518EA41E, ZelemSpecialHaster 0xB28B12F2). Note: capture enemies sit far from spawn ((-156,-62) area) — "nothing visible" may partly be distance; spawn-pos log added for the client gate. Golden tests byte-match capture reps (enemy 80B body, teleporter 92B body). 69/69. Client gate pending. | fixed | 32a7719 |
| D-021 | CrystalDragMessage (0xC2) + CrystalMessage (0xC3) | 11 | OnCrystalDragMessage (Server.cpp:1026-1091): 6×u32 in; empty slot/no creature → SendCrystalMessage(3, slot) reject; moveType 2 = grid swap; 0 = DropCatalyst world drop. Writer Server.cpp:2302-2324 | CrystalDragMessagePacket + CrystalMessagePacket + Game.HandleCrystalDrag | Was the last unhandled real inbound. Client ground-truth (Ghidra): 0xC3 body = **fixed 29B** (ClientNet::OnGmsCrystalMessage @0x0053f700 reads exactly 0x1d; C++ source's 33B moveType-0 branch is wrong-as-written — the trailing zeros make it parse, client only consumes 29B); reject (3) echoes the dragged slot at +21 → client snaps the crystal back; client sender = nPlayer::PickupCrystal @0x009ff6d0. v1: parse + always-reject — byte-identical to C++ behavior with an empty catalyst grid, which is the only state until the loot phase (catalysts only enter the grid via loot pickup, Instance.cpp:1103). Grid swap (2) + drop-to-world (0, reveal layout nounId/rarity/color ready in the packet) land with loot. Tests 67/67 (29B reject/swap/reveal + 24B parse). | fixed | b5ab032 |
| D-020 | ActionCommand full type set + command lock | 11 | C++ handles 3/4/5/7(partial)/9/10/11/12/13 (Server.cpp:688-981) but never sends the lock-clearing response for emotes ("TODO: proper function for dancing"); type 6 absent entirely | Game.HandleActionCommand + ActionCommandResponsePacket + ActionCommandMsgsPacket | **Dance-freeze root cause (client gate 2026-06-05 16:27): after type 12/13 the client armed its 3s pending-command lock (FUN_004d9150 → block@DAT_0143ffd8+0x50) and hard-rejected all movement clicks — zero packets sent — because we never sent ActionCommandResponse (0xA8).** Ghidra sweep (3 agents) pinned the full client contract (VERIFIED_FACTS "ActionCommand contract"): commandStamp = inbound header byte +0x01 (rolling counter DAT_011205e0, enqueue-path cmds 5/6/9/11/12/13 only; movement carries none); response 56B type=2 with stamp echo at [0] + userData=0xFFFFFFFF clears the lock (FUN_004d9ba0 case 2) AND resets the anim — so the response goes FIRST, the emote SetAnimationState after. Implemented: 12/13 emote contract (response→anim, `_activeEmotes` tracking, SetAnimationState(0) cancel on next 3/4/5/10 — emote anims loop forever, anim flag +0x70=0); **6 Overdrive** (u8 heroSlot, client-only type, parse + ack stub); **7/8 abilities** (44B Types.h:855 parse + type=2 ack — deliberately NOT the case-1 ability ack: that path drives locomotion/FX from response fields → Simulation-phase work); **9 CatalystPickup** (20B parse → interact bookkeeping + ack); **10 Cancel** (emote clear; C++ only stops Lua threads); 11 now also acks. Types 1/2 have no client emit path — left unimplemented by design. Header re-verified: +0x04 = locomotionFlags (C++ Types.h:831 misnames it inputSyncStamp), +0x08 = objectId-in-practice. Tests 63/63 (56B response offsets + all payload parsers). ✅ CLIENT-VERIFIED 2026-06-05 ("boa, agora ta ok"): dance no longer freezes input. | **verified** | 3c77742 |
| D-019 | ActionCommand types 11/12/13 | 11 | Type 11 → Instance::InteractWithObject (Instance.cpp:541-578: TimesUsed++ → 0x98; Loot/Crystal pickups; default DropLoot = dev shortcut); 12/13 → SendAnimationState 0xA5 25B (Server.cpp:966-977/1989-2001; emote_dance_all/emote_taunt_all, overlay=false scale=1) | Game.HandleActionCommand + SetAnimationStatePacket (new) + InteractableDataUpdatePacket | Were unhandled. v1: type 11 = TimesUsed tracking + 0x98 reply (loot-drop branch deferred to loot phase); 12/13 = full emote contract. Golden test 0xA5. Client gate pending (dance/taunt visible; obelisk reacts). | fixed | 41c7021 |
| D-018 | SwitchCharacter (ActionCommand type 5) | 11 | C++ reads u32 creatureIndex → Player::SwapCharacter (CurrentDeckIndex + dataBit + PlayerBits, Player.cpp:234) → broadcast PlayerCharacterDeploy → LPU (Instance.cpp:523-539) | Game.HandleActionCommand type 5 | Was a log-only stub. Implemented to the C++ contract (validates index vs deck, updates _playerCharacterObjectIds, reuses the deploy SwapCharacter helper). Same commit adds `Game.TeleportMovement` (mirror of C++ Server.cpp:76); follow-up inverted the default per user: **smooth is the DEFAULT** (walking is the game's normal — D-017b client-confirmed: undeployed heroes walk on their own), CLI `--teleport-movement` re-enables the D-015 snap contract as fallback. ✅ CLIENT-VERIFIED 2026-06-05: smooth controlled-hero movement + character switch BOTH working ("ta tudo funcionando"). | **verified** | 2460604+33df231 |
| D-017 | Hero spawn 0x96 attribute set | 10 | Object setup fills base ClassAttributes when >0 (Object.cpp:608-662) + SetWeaponDamage(1,5); wire: cpp_loopback 0x96 hero msgs #607/612/617 = 15 attrs {0 Str,1 Dex,2 Mind,4 MaxHP,5 MaxMana,7 PhysDef,9 EnergyDef,10 Crit,**11 NonCombatSpeed=8.25,12 CombatSpeed=7.5**,23,24,109,111,112}; binary uses 111/112 for weapon dmg (source enum 101/102 DRIFTED — trust wire) | Game.OnPlayerStart + GameService.ToSquadCreature + AttributeDataUpdatePacket | C# sent only 7 attrs — **no movement speeds (11/12)**: client-side hero had no NonCombatSpeed/CombatSpeed, starving the client-authoritative movement validation/sim mapped in CLIENT_MOVEMENT_CONTRACT.md. FIX: SquadCreature carries 8 base stats from ClassAttributes (AssetData-derived, >0-gated like C++); spawn 0x96 now mirrors the capture set. Golden test = byte-exact capture msg #612. **Client gate 2026-06-05 11:16: attrs alone do NOT unlock self-walk** (no-teleport experiment 7ea9536: position frozen; reverted 7a45d44 → teleport contract restored). D-017 stays a correct fidelity fix (capture parity), but the smooth-walk blocker is elsewhere (permission bitmask FUN_009c7620 / component creation / a path not yet mapped). The 11:16 gate was a FALSE NEGATIVE: ClassAttributes resolution silently returned null (`GetNoun(wireId)` never resolves — dbpf hash domains) so the speeds were 0 on the wire. D-017b (9a840a8) added the noun wire-id index; with speeds actually resolving, ✅ CLIENT-VERIFIED: heroes walk (undeployed heroes even walk on their own). C++ cross-check (user): with teleportMovement=false, 2 of 3 creatures walk; the non-walker (0x2ca50a9a) likely lacks baseNonCombatSpeed in its serverdata XML — same mechanism, both sides. | **verified** | bcaf645+9a840a8 |
| D-016 | ActionCommandResponse type 8 (smooth movement) | 11 | Client `OnGmsActionCommandResponse`@0x0053cb10 (56B, type byte@+1, decoded via FUN_004d9ba0): type 8 → client executes its click-stashed goal via `Locomotion::SetGoalPositionWithDistance` = retail smooth-move protocol. C++ only sends type 1 (ability ack, Server.cpp:1659) — never implemented type 8, hence teleportMovement | Game.HandleActionCommand + ActionCommandResponsePacket | **❌ CLIENT-REFUTED for plain movement (2026-06-05 09:19 run: position frozen, zero movement).** Reinterpretation: case 8 requires the pending-command stash (`pending+4 != 0`), which only exists when the click routed through the deferred-ability path (FUN_004d9150 branch 1) — i.e. **type 8 = "approach-target GO" for a deferred ability** (walk into cast range), NOT plain click-to-move. Plain ground clicks evidently don't create the stash. Reverted Game.cs to the D-015 teleport contract (kept: rewritten ActionCommandResponsePacket — fixes latent 4B-packet bug — + golden test; the packet is correct for its real purpose, ability flows will need it). Plain smooth movement still open: candidates = LocomotionDataUnreliableUpdate 0x95 (ungated goal write, but doesn't set goalFlags — may need pre-set flags state) and/or the local-execution branch (cmd-def +0x151). **Decisive next = runtime trace** (Ghidra/CE attach): (a) is FUN_004d9150 even called on ground click; (b) ValidateCommandRoute return; (c) any Locomotion setter hit on click; (d) GameSimulator local-handling path. | refuted (packet kept) | c0534d6+revert |
| D-015 | ObjectPlayerMove GoalFlags | 11 | `Locomotion::SetGoalPosition` forces `mGoalFlags=0x001` + resets target/facing/velocity (Locomotion.cpp:477-491); Movement handler ORs the client's flags on top (Server.cpp:726-728); `Stop()` sets 0x020 **preserving GoalPosition** (Locomotion.cpp:564-575); tick re-sends 0x91 for player-controlled objects (Instance.cpp:967-970) | Game.HandleActionCommand (Game.cs:756) | **Player didn't move on click (log 2026-06-04: ActionCommand type=3 arrives, 0x91 reply sent with `flags=0x0`).** C# passed the client's raw `goalFlags` (=0x0) straight into 0x91 — without bit 0x001 the client locomotion has no active goal and ignores the move. Stop path also zeroed GoalPosition (C++ preserves it across Stop). FIX: `LocomotionData.SetGoalPosition()/Stop()` reimplemented to C++ semantics + per-object persistent `LocomotionData` map in Game; PartialGoalPosition = common-data position (C++ Server.cpp:727). **CLIENT GATE 2026-06-05: flags=0x1 alone does NOT move the client.** Wire-diff of cpp_loopback (working binary) settled the real contract: every click → **0x90 ObjectTeleport (pos=goal, quat=ZERO not identity) + 0x91 flags=0x021** — the binary runs `teleportMovement=true` (Server.cpp:76, "TESTING ONLY" but it IS the working behavior; per source flow 0x020 forces the teleport path, Server.cpp:722-724). Movement on screen = the teleport snap; flags 0x001-only smooth locomotion is an unverified path (likely needs server-side movement sim) — do NOT "improve" to it without new evidence. FIX 2: handler ORs 0x020 + sends 0x90 then 0x91 per capture order; golden tests now byte-match capture msgs #926/#928. **✅ CLIENT-VERIFIED 2026-06-05 07:40: hero moves (teleport snap per click); client's reported position updates every command (-600→-474 across the run) + sends Stop(4) on arrival — the movement loop is alive.** Smooth ("real") movement status: C++ NEVER implemented it — `Locomotion::Update` Pathfinding case is an empty `if(false)` stub (Locomotion.cpp:147-155; only Projectile/OrbitOwner simulate) — that's why `teleportMovement` exists. **GHIDRA-RESOLVED 2026-06-05 (docs/architecture/research/CLIENT_MOVEMENT_CONTRACT.md):** 0x91 has a LOCAL-HERO GATE (`ClientNet::OnGmsObjectPlayerMove`@0x0053e1c0 ignores own-hero moves unless CC'd) — flags=0x001 can never move the local hero by design (round-1 fully explained: deferred command + gated 0x91 + 3s deadline expiry); it IS the smooth channel for enemies/others. Smooth own-hero route = `LocomotionDataUnreliableUpdate` (16B objId+goal, handler @0x0053e600, UNGATED, seeds local Locomotion sim) — experiment pending. Separate future row. Residual gap also open: C++ `MoveObject` updates server-side `object->SetPosition(...)` (Instance.cpp:495/500); C# keeps no object-position state yet. New unhandled inputs seen in log: ActionCommand type=7 (UseCharacterAbility), type=10 (Cancel). | **verified** | 395136f+9f93acb |
| D-012 | GameState game-mode field | 11 | RakNet `Server::SendGameState` writes `Blaze::GameType::Chain` (=2) every tick (Server.cpp:1369 → client `simulator+0x3B420`, default 0xFFFFFFFF) | GameStatePacket.GameType, set in Game.Update (Game.cs:66) | **CRASH ROOT CAUSE (the cMapRoomUI @0x551f47 crash).** C# hardcoded `GameType = 0` in the per-tick GameState broadcast; **0 is not a valid `Blaze::GameType` member** (enum = Tutorial=1, Chain=2, Arena=3, … Types.h:158-166). The client stores this game-mode at `simulator+0x3B420` and dispatches its UI state machine off it; an invalid mode pushes it down the matchmaking/map-room path → `cMapRoomUI` activates (UI-mode 3) but its Scaleform movie (FE_MM2b.gfx, +0x28) isn't bound for that context → null-movie `Movie_InvokeMethod` → ACCESS_VIOLATION read 0x0. Same retail client works under C++ precisely because C++ sends Chain(2). Note: name collision — C# already had an unrelated Blaze-matchmaking `GameType {Managed,Matched,Solo}` enum (IGame.cs); the RakNet field needed the game-MODE enum. **FIX:** added `LabsGameType` enum (mirror of C++ Blaze::GameType) in GameStatePacket.cs; Game.Update now sends `LabsGameType.Chain`. Build green, 37/37 golden. **⚠ CLIENT-VERIFY 2026-06-04 15:43: crash PERSISTS byte-identical (0x551f47, edi=0, cMapRoomUI, same stack). Wire CONFIRMS the fix is live (GameState now `… 02 | 02 00 00 00`, game-type=2, was 0).** So game-mode is a genuine fidelity divergence (keep the fix, matches C++) but was NOT the crash trigger. Like D-010, the cMapRoomUI null-movie crash is **insensitive to gameplay-state content** (survives correct game-type AND the D-011 world cleanup). Real trigger still open — strongest remaining lever = the Blaze **Rooms** flow (mid-game joinRoom→NotifyRoomAdded/Updated/MemberJoined) that could activate the matchmaking/map-room UI, OR a client UI init-order issue. Decisive next = OBSERVE (runtime BP on movie-load 0x51cc50 / bind 0x51c360 / UI-mode setter 0x4e91e0 capturing value+caller) — no more blind guesses. | fixed (not the crash) | |
| D-011 | Level object population | 10 | Instance::OnPlayerStart 3 filtered passes (Instance.cpp:318-391); TriggerVolume ctor = SecurityTeleporter.noun + SetScale(0) + SetHasCollision(false) (ObjectManager.cpp:13); Create(marker) sets pos/orient/markerId, scale only for non-triggers (ObjectManager.cpp:206-212); load loop sends ObjectCreate ONLY (Instance.cpp:403-410); InteractableDataUpdate 0x98 fires only on runtime loot drops (Instance.cpp:663-667) | Game.cs PopulateLevel | **BLACK-CUBES ROOT CAUSE (user-observed 2026-06-04).** C# spawned EVERY level marker — scaldron_4: 3555 ObjectCreate, `skipped 0`: binary markersets serialize `componentData` (all 10 sub-nodes) on every marker, so the XML-presence filter never skipped anything — including design-blockout meshes (`PHYS_design_object_block_3x3`, 171-marker `design_blocks` set, `TestDoor_design_blockin_*`) and editor nouns (`SpawnPoint_Director*` ×554, `CameraSpawnPoint`, `MapCamera`, `scaldron_4!path*`) → client renders blockout/placeholder cubes next to the heroes. Also sent a bogus 0x98 companion per object — the capture's 0x98×278 (D-009) was full-session loot traffic, not load contract. **Fixed to the C++ 3-pass contract:** obelisks by noun name (`prefab_health/boss_obelisk.Noun`) from `_obelisk_1`; `_design`/`_design_spawners` markers with non-empty `teleporter` component → SecurityTeleporter.noun scale-0 no-collision trigger; ≤1 director enemy per `_AI_Wander*` set from the ChainData enemy bank (scaldron_4's `_AI_Wanderers`/`_AI_spike_*`/`_AI_Horde_*` names don't match the C++ candidate list → 0 enemies, exact C++ parity); no 0x98 at load. Census/diag tool: `tools/MarkerScan`. Expected scaldron_4 log: 5 obelisks + 3 teleporter triggers + 0 enemies. Latent follow-ups (separate rows when tackled): **hash-domain rule VERIFIED via MarkerScan probes** — DBPF `InstanceId = FnvHash(bare name, no extension)` (`prefab_health_obelisk` YES / `...obelisk.Noun` NO; same for SecurityTeleporter), wire noun id = FnvHash(name WITH extension) ⇒ `GetNoun(wireId)`/`GetNounByName(name.Noun)` NEVER resolve (ObjectManager.Spawn maxHealth/aiDef/**hasLocomotion** silently no-op — blocks Phase 2b ObjectTeleport gating), while `GetMarkerSetByName` (called with bare names) WORKS — supersedes the 2026-05-31 "GetMarkerSetByName still latent" claim; `_AI_Wanderers` naming gap blocks any wander enemies on scaldron-family levels (needs verified design before improving beyond C++). **VERIFY GATE 2026-06-04 14:36/14:37 (two client runs):** live log `PopulateLevel(scaldron_4): 5 obelisks, 3 teleporter triggers, 0 director enemies` — exactly the predicted counts; load visibly faster. Crash UNCHANGED: ACCESS_VIOLATION read 0x0 @0x00551f47, stack 0045778a→005399b4→007eedc0, ~2s post-deploy, mid-game joinRoom at +1.05s — byte-identical to D-010 signature ⇒ object stream re-exonerated for D-003 with the cleanest world yet (8 objects, zero 0x98). Client-side field-table validation (Ghidra 2026-06-04): hero {0,1,3,17} / world {6,7,8,17,22} / update {6,16} ids all exact per `AssetData::RegisterSporelabsObjectFields` @0x00f8ded0 (see VERIFIED_FACTS). | verified | |
