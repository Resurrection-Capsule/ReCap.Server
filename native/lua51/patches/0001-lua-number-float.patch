diff --git a/native/lua51/lua-5.1.4/src/luaconf.h b/native/lua51/lua-5.1.4/src/luaconf.h
index e2cb261..27324e7 100644
--- a/native/lua51/lua-5.1.4/src/luaconf.h
+++ b/native/lua51/lua-5.1.4/src/luaconf.h
@@ -501,8 +501,8 @@
 ** ===================================================================
 */
 
-#define LUA_NUMBER_DOUBLE
-#define LUA_NUMBER	double
+/* ReCap: Darkspore chunks are float (header byte 10 = 4, client 0x00909cb0) */
+#define LUA_NUMBER	float
 
 /*
 @@ LUAI_UACNUMBER is the result of an 'usual argument conversion'
@@ -518,11 +518,11 @@
 @@ LUAI_MAXNUMBER2STR is maximum size of previous conversion.
 @@ lua_str2number converts a string to a number.
 */
-#define LUA_NUMBER_SCAN		"%lf"
-#define LUA_NUMBER_FMT		"%.14g"
+#define LUA_NUMBER_SCAN		"%f"
+#define LUA_NUMBER_FMT		"%.7g"
 #define lua_number2str(s,n)	sprintf((s), LUA_NUMBER_FMT, (n))
 #define LUAI_MAXNUMBER2STR	32 /* 16 digits, sign, point, and \0 */
-#define lua_str2number(s,p)	strtod((s), (p))
+#define lua_str2number(s,p)	((lua_Number)strtod((s), (p)))
 
 
 /*
